name: 'Build Maven settings for deployment'
description: 'Build Maven settings for deploying artifacts'
inputs:
  version-type:
    description: 'Version type (release/develop)'
    required: true
    default: 'develop'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.version-type }}